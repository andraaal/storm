use crate::context::Context;

impl Context {
    /// Checks for and executes all state-based actions, returning true if any were executed
    pub(crate) fn exec_state_based(&mut self) -> bool {
        
    }
}
